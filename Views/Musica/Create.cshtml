@model SoundFy.Models.Musica

@{
    ViewData["Title"] = "Create";
}

<h1>Adcionar músicas</h1>

<h4>Música</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="NomeMusica" class="control-label"></label>
                <input asp-for="NomeMusica" class="form-control" />
                <span asp-validation-for="NomeMusica" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="AlbumId" class="control-label"></label>
                <select asp-for="AlbumId" class ="form-control" asp-items="ViewBag.AlbumId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Adcionar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Votlar a lista de músicas</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
